# Tourist AI

```
cp backend/.env.example backend/.env
```

Then, fill in the necessary environment variables in `backend/.env` file.`

```
docker compose up -d --build
```
```
npm install
```

```
npx expo run:android
```

There is also an option to add a photo from the gallery.